import { ConvertCoinPipe } from './convert-coin.pipe';

describe('ConvertCoinPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertCoinPipe();
    expect(pipe).toBeTruthy();
  });
});
