import { ChangeColorTextDirective } from './change-color-text.directive';

describe('ChangeColorTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeColorTextDirective();
    expect(directive).toBeTruthy();
  });
});
