import { IterateListDirective } from './iterate-list.directive';

describe('IterateListDirective', () => {
  it('should create an instance', () => {
    const directive = new IterateListDirective();
    expect(directive).toBeTruthy();
  });
});
